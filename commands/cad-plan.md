---
name: cad-plan
description: Create structured implementation plan as native UI Artifact with zero Git clutter.
---

Execute the `cadence-plan` skill:
1. Survey the feature or refactor scope (delegating deep reads to `cadence-scout`).
2. Generate an Antigravity UI Plan Artifact (`brain/<conversation-id>/`) with:
   - Objective & Scope
   - Mermaid Architecture / Flow diagram
   - TDD Red-Phase contract strategies
   - Sequenced task checklist tagged with construction emojis (🟢, 🟡, 🔴, 🏗️)
   - Parallel subagent allocations
3. Align with user on architectural options.
