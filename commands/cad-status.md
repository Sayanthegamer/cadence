---
name: cad-status
description: Instant project standup & status overview (Where did we leave off?).
---

Execute the `cadence-status` skill:
1. Run `git branch --show-current`, `git status -s`, and `git log -n 3 --oneline`.
2. Check for active Antigravity Plan Artifacts and `.agents/decisions/`.
3. Provide the concise 3-bullet status briefing: Completed, In-Flight, and Next Immediate Action.
